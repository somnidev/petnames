# Petnames

In order to use [golang petname](go get github.com/dustinkirkland/golang-petname) library we have to execute the following command on the command line.

```bash
go get github.com/dustinkirkland/golang-petname
```
